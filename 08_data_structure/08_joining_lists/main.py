#lists
    # stores more than one information inside variables
    # they keep the data sequence inside a variable.
    # square brackets = []

colors=['yellow','red','black','blue']
values=[10, 20, 30, 40]

two_lists = zip(colors,values)
print(list(two_lists))

# var=list('buy')
# print(var)