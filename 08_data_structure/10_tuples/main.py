#Tuples
    # Tuples CANNOT BE MODIFIED! (immutable)
    # stores more than one information inside variables
    # they keep the data sequence inside a variable.
    # parentheses = ()

color_list = ['yellow','green','blue','red']
color_tuple = ('yellow','green','blue','red')

print(color_list)
print(color_tuple)

print(type(color_list))
print(type(color_tuple))